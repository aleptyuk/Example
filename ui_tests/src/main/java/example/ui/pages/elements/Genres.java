package example.ui.pages.elements;
import example.ui.pages.BasePage;
import org.openqa.selenium.WebDriver;
public class Genres extends BasePage {
    public Genres(WebDriver driver) {
        super(driver);
    }
}
